import { FormulaItem } from './index'

const fonts = [
  {
    name: '因为',
    icon: 'because',
    formula: '\\because',
  },
  {
    name: '所以',
    icon: 'therefore',
    formula: '\\therefore',
  },
] as FormulaItem[]

export default fonts
